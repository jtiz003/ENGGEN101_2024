my_array = ["fish", "cat", "dog"]
tab = ",\t"

# Access array values with their position
print(my_array[0] + tab + my_array[1] + tab + my_array[2])

# Set array values with the position
my_array[0] = "frog"
print(my_array[0])

#Append new values to the end of an array
my_array.append("bird")
print(my_array)

